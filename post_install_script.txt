sudo pacman -Sy;
sudo pacman -S --needed --noconfirm git timeshift nvidia-dkms nvidia-utils lib32-nvidia-utils nvidia-settings vulkan-icd-loader lib32-vulkan-icd-loader packagekit-qt6 flatpak fwupd ffmpegthumbs unrar kaccounts-integration kaccounts-providers kio-gdrive kimageformats amd-ucode icoutils libreoffice-fresh gwenview spectacle wine-staging wine-gecko wine-mono lutris steam fuse fuse2 opencl-nvidia audacity base-devel git reflector qt5-imageformats kimageformats gimp yt-dlp exfat-utils linux-headers linux-zen-headers linux-lts-headers vi partitionmanager mangohud gamemode vivaldi qbittorrent android-udev chromium system-config-printer cups okular syncthing kdeconnect;
systemctl enable cronie.service;
sudo systemctl enable cups.service;
sudo systemctl start cups.service;
sudo systemctl start bluetooth;
sudo systemctl enable bluetooth;
git clone https://aur.archlinux.org/yay.git;
cd yay;
makepkg -si;
yay -S pamac-aur;
yay -S cloudflare-warp-bin;
sudo systemctl start warp-svc;
sudo systemctl enable warp-svc;
warp-cli register;
sudo cp /etc/pacman.d/mirrorlist /etc/pacman.d/mirrorlist.bak;
sudo reflector --verbose --latest 10 --protocol https --sort rate --save /etc/pacman.d/mirrorlist;
yay -S --needed libxcrypt-compat;
flatpak install net.davidotek.pupgui2;
